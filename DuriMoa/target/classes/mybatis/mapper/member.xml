<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ugi.durimoa.member.dao.IMemberDAO">

	<select id="loginMember" parameterType="MemberVO"  resultType="MemberVO">
		SELECT *
	    FROM member
	    WHERE mem_id = #{memId}
	    AND mem_pw = #{memPw}
	</select>
	
	<insert id="registMember" parameterType="MemberVO">
		INSERT INTO member(mem_id, mem_pw, mem_nm, mem_img) 
		VALUES (#{memId}, #{memPw}, #{memNm}, #{memImg})
	</insert>
	
	<update id="updateMember" parameterType="MemberVO">
		UPDATE member
		SET mem_pw = #{memPw},
		    mem_nm = #{memNm},
		    mem_img = #{memImg}
		WHERE mem_id = #{memId}
	</update>
	
	<select id="idCheck" resultType="int" parameterType="String">
		SELECT COUNT(*) FROM member
		WHERE mem_id = #{memId}
	</select>

	<select id="coupleck" resultType="MemberVO" parameterType="String">
		SELECT mem_id, mem_nm, mem_img FROM member WHERE mem_id = #{memId}
	</select>

	<insert id="coupleAdd" parameterType="CoupleVO">
		INSERT INTO couple(cop_id, cop_nm, cop_dt)
		VALUES(sq_cop_id.NEXTVAL, #{copNm}, #{copDt})
		<selectKey keyProperty="copId" resultType="int" order="AFTER">
			SELECT sq_cop_id.CURRVAL FROM dual
		</selectKey>
	</insert>
	
	<update id="updateCop" parameterType="MemberVO">
		UPDATE member
		SET cop_id = #{copId}, cop_yn = 'Y'
		WHERE mem_id= #{memId}
	</update>
	
	<select id="copSession" resultType="CoupleInfoVO" parameterType="MemberVO">
		SELECT 
		    b.mem_id,
		    b.mem_nm,
		    b.mem_img,
		    c.cop_id,
		    c.cop_nm,
		    c.cop_dt
		FROM MEMBER a, MEMBER b, COUPLE c
		WHERE 
		    a.cop_id = b.cop_id
		    AND a.mem_id != b.mem_id
		    AND a.mem_id = #{memId}
		    AND a.cop_id = c.cop_id
	</select>

	<update id="copUpdate" parameterType="CoupleVO">
		UPDATE couple SET cop_nm = #{copNm}, cop_dt = #{copDt}
		WHERE cop_id = #{copId}
	</update>
</mapper>